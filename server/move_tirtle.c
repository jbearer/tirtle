#include "path.h"
#include "motor_control.h"

static path_t *path;
static 

/* load image paths and store them for use */
void load_image(path_t *path_, length_t num_paths);
{
  paths = path_;
  
}

set_position(point_t loc, angle_t angle);

